import { MdxLayout } from '@/layouts/mdx-layout'
import { Counter } from '@/components/counter'

export const meta = {
  title: 'About',
}

export default MdxLayout

Well, I thought the `MdxLayout` would replace the default layout. Instead,
`MdxLayout` remains a child of `Layout` and simply wraps this document.

## Code Example

```typescript:greeting.ts
function greeting(message: string): string {
  return `Hello! ${message}`
}
```

```json
{
  "name": "david"
}
```

## Component Example

<Counter />

The `Counter` component above is inheriting the `prose` styles defined in `MdxLayout`. There is no way to turn off `prose` styling for a specific tag and its children. So, components placed within a `prose` container must be mindful of the `prose` styling influence.
